mod utils;

// pub fn render(item: Item) -> RenderCommand[] {
//   // get current state of item
//   // Find the current visible image from all possible images
//   // Find the coordinates where the image has to be rendered
//   // Add command to render stack
// }

// pub fn runCommands(cmds: RenderCommand[], canvas: web_sys::HtmlCanvasElement) {

// }
